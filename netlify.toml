[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: all routes serve the built app (safe for client-side navigation later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
